{
  "name": "splits-html-manager",
  "version": "1.0.0",
  "description": "A lightweight Windows 10 inspired tool to split and combine HTML, CSS, and JS files.",
  "main": "html_splitter_combiner.html",
  "scripts": {
    "lint": "npx eslint .",
    "lint:fix": "npx eslint --fix .",
    "format": "npx prettier --write .",
    "test": "echo 'Error: no test specified' && exit 1",
    "prerelease": "npm run lint:fix && npm run format",
    "start": "npx serve ./src/"
  },
  "keywords": [
    "html",
    "css",
    "javascript",
    "tool",
    "splitter",
    "combiner"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^17.2.0",
    "prettier": "^3.0.0",
    "@eslint/js": "^9.0.0"
  },
  "dependencies": {
    "dompurify": "^3.3.1"
  }
}
